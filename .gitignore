# Secrets — never commit
.env
credentials.json
token.json

# Disposable intermediates
.tmp/

# Student data banks — hold personal info (PDPA), keep out of git
data/students/

# Google Form CSV exports — hold personal info (PDPA), keep out of git
data/form/

# Python
.venv/
__pycache__/
*.pyc
*.pyo

# OS / editor
.DS_Store
Thumbs.db
